ospf:
  ospfv3:
    06-lb-prefix:
      caveat: SR Linux advertises IPv6 loopback prefixes with /64 prefix length (see RFC 5340, section 4.4.3.9)
bgp:
  05-community:
    caveat: SR Linux does not differentiate between standard (2-byte ASN ) and large (4-byte ASN) BGP communities
