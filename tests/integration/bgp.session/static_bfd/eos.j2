{% for ngb in bgp.neighbors if 'ipv4' in ngb %}
{%   for intf in interfaces if intf.ifindex == ngb.ifindex %}
!
interface {{ intf.ifname }}
  bfd static neighbor {{ ngb.ipv4 }}
{%   endfor %}
{% endfor %}
