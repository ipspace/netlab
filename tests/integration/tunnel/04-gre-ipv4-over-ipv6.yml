---
message: |
  Test GRE tunnel can encapsulate IPv4 in IPv6 underlay.
  The two tested devices have a GRE tunnel and run OSPF over it.
  The probe (R3) should receive an OSPF route advertised from the
  remote device (DUT_R1) and be able to ping its loopback.

defaults.sources.extra: [ ../wait_times.yml, ../warnings.yml ]

addressing:
  core:
    ipv4: false
    ipv6: 2001:db8:a::/48
  p2p:
    ipv4: 172.16.0.0/16
  end:
    ipv4: 172.16.0.0/16

plugin: [ tunnel.gre ]

groups:
  probes:
    device: frr
    provider: clab
    members: [ core, r3 ]
  ce:
    members: [ dut_r1, dut_r2 ]
    routing.static:
    - pool: core
      nexthop.node: core

module: [ ospf, routing ]

nodes:
  dut_r1:
  dut_r2:
  core:
    module: []
  r3:

links:
- group: core
  ospf: False
  role: core
  pool: core
  members: [ dut_r1-core, dut_r2-core ]
- group: site
  pool: end
  members: [ dut_r2-r3 ]
- group: gre
  tunnel.mode: gre
  tunnel.af: ipv6
  tunnel.source.link.role: core
  pool: end
  members: [ dut_r1-dut_r2 ]

validate:
  adj_v2:
    description: Check OSPFv2 adjacencies
    wait: ospfv2_adj_p2p
    wait_msg: Waiting for OSPF adjacency process to complete
    nodes: [ r3 ]
    plugin: ospf_neighbor(nodes.dut_r2.ospf.router_id)
  pfx_v2:
    description: Check for DUT_R1 loopback prefix via OSPFv2 being propagated to R3
    wait: ospfv2_spf_long
    wait_msg: Waiting for SPF to do its magic
    nodes: [ r3 ]
    plugin: ospf_prefix(nodes.dut_r1.loopback.ipv4)
  ping4:
    description: Check end-to-end connectivity GRE 4 over 6
    wait: ping
    wait_msg: Still waiting for SPF...
    nodes: [ r3 ]
    plugin: ping(nodes.dut_r1.loopback.ipv4)
