#
# Anycast gateway test case
#

gateway.id: 1

groups:
  switches:
    module: [ gateway,vlan ]
    members: [ r1, r2, r3 ]
    device: eos
  hosts:
    members: [ h1, h2, h3, h4 ]
    device: linux

nodes: [ r1,r2,r3,h1,h2,h3,h4 ]

vlans:
  red:
    gateway.anycast.unicast: True

links:
- r1:
  r2:
  h1:
  h2:
  gateway: True
- r1:
  h3:
  vlan.access: red
- r2:
  h4:
  vlan.access: red
