---
message: |
  Multi-topology underlay: transport VRF links between DUT routers and the probe node.

groups:
  probes:
    device: frr
    provider: clab
    members: [ r3 ]
  ce:
    members: [ dut_r1, dut_r2 ]

vrfs:
  transport:

nodes:
  dut_r1:
  dut_r2:
  r3:

links:
- group: core
  ospf: False
  vrf: transport
  members: [ dut_r1-dut_r2 ]
- group: site
  members: [ dut_r2-r3 ]
