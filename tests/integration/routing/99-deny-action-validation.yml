---
message: Test deny action validation

module: [ routing ]

nodes:
  dut:
    device: frr
    routing.policy.test_deny:
    - action: deny
      match:
        aspath: '65001'
      set:
        community:
          standard: '65000:100'
