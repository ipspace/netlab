#
# Nokia SR Linux tests
#
provider: clab
defaults:
  device: srlinux
  bgp:
    as: 65000

module: [ ospf, bgp ]

nodes:
  s1:
    bgp:
      as: 65100
  s2:
  s3:
    device: sros
    license: /Projects/SR_OS_VSR-SIM_license.txt

links: [ s1-s2, s1-s3, s2-s3 ]
