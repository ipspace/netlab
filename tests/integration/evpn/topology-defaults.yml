vxlan.start_vni: 5000
evpn.start_transit_vni: 10000
paths.plugin: [ "topology:.", "topology:../plugin", "package:extra" ]

groups.switches.role: router            # All EVPN switches have to be routers
groups.core.role: router                # ... as well as all core devices
