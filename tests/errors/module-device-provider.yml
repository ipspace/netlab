---
defaults.devices.linux:
  clab.image: constant
  libvirt.image: constant

defaults.device: linux

nodes:
  sw1:
    module: [ vlan ]
    provider: libvirt
  sw2:
    module: [ vlan ]
    provider: clab
