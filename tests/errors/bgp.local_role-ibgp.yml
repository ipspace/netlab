#
# BGP local role cannot be used on IBGP sessions
#
plugin: [ bgp.local_role ]
module: [ bgp ]

nodes:
  r1:
    device: frr
    bgp.as: 65000
    bgp.local_role: provider
  r2:
    device: frr
    bgp.as: 65000

links:
- r1-r2
