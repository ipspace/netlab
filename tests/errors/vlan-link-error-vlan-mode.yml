module: [ vlan ]
defaults.device: iosv

vlans:
  red:

nodes:
  n1:
  n2:

links:
- n1:
  n2:
  vlan.mode: irb      # Invalid, only 'route' allowed on link

- n1:
  n2:
    vlan.mode: irb    # Invalid, only 'route' allowed on interface
