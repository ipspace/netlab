#
# RIPv2 cannot run over unnumbered interfaces on devices that don't support it
#

defaults:
  devices.csr.features.ripv2.unnumbered: False

defaults.device: csr

module: [ ripv2 ]

nodes:
  r1:
  r2:

links:
- r1:
  r2:
  unnumbered: True
