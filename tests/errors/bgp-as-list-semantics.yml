#
# BGP AS member errors
#
module: [ bgp ]
defaults.device: none

bgp:
  as_list:
    65001:
      members: [ r1 ]
    65003:
      members: [ r*, x* ]
      rr: [ r*, '~a[12]' ]
    65004:
      members: [ a1, a*, '~*', '~b.*' ]

nodes: [ r1, r2, r3, a1, a2, a3 ]
