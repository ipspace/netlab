---
plugin: [ bgp.policy ]
module: [ bgp ]

defaults.device: none
defaults.bgp.as: 65000

nodes:
  r1:
    bgp.as: 65001
  r2:
    bgp.as: 65002

links:
- r1:
    bgp.role:
      strict: true
  r2:
    bgp.role: customer
