#
# A 2:1 LAG (M-side) requires MLAG or ESI-LAG on the pair of switches.
#

defaults.device: none

module: [ lag ]

nodes:
  s1:
    _features.lag.mlag: False
  s2:
    _features.lag.mlag: False
  h1:

links:
- lag.members: [ h1-s1, h1-s2 ]
