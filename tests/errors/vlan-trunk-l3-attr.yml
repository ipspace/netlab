defaults.device: eos
provider: clab
defaults.vlan.warnings.trunk_attribute: error

module: [ vlan, ospf ]

vlans:
  red:
    mode: route
  blue:
    mode: route

nodes:
  a:
  b:

links:
- a:
    ospf.cost: 10
  b:
  vlan.trunk: [ red, blue ]
  vlan.native: red
  ospf.cost: 20
