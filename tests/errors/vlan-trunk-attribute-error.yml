module: [ vlan ]
defaults.device: iosv

vlans:
  red:
  blue:

nodes: [ n1, n2 ]

links:
- n1:
  n2:
  vlan.trunk:
    red:
    blue:
      bgp.as: 65000  # 2 issues here: bgp is not an enabled module, and "bgp.as" is not a valid interface attribute for irb vlans
