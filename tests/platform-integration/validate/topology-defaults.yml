provider: clab

groups:
  grp_eos:
    members: [ dut_eos ]
    device: eos
  grp_frr:
    members: [ dut_frr ]
    device: frr
  grp_linux:
    members: [ dut_linux ]
    device: linux
  grp_linux_probes:
    members: [ h1, h2, h3 ]
    device: linux
  grp_frr_probes:
    members: [ r1, r2, r3 ]
    device: frr
