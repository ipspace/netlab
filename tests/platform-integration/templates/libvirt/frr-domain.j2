### Custom device template
    {{ name }}.vm.synced_folder ".", "/vagrant", id: "vagrant-root", disabled: true
    {{ name }}.vm.provider :libvirt do |domain|
      domain.cpus = 1
      domain.memory = 1024
    end

# The rest of the file intentionally left blank ;)
