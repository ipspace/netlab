message:
  The test creates all three types of libvirt links (stub, p2p, multi-access)
  with one of the nodes being an unmanaged nodes

provider: libvirt
defaults.device: linux

nodes:
  a:
  b:
  c:
  x:
    unmanaged: true

links:
- x
- a-x
- a-b-x
- a-b-c-x
