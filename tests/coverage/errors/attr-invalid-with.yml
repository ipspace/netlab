# Test the "normalize and merge" routing policy processing
#

module: [routing]
defaults.device: none

routing.policy:
  p1:
  - locpref: 10
    action: deny
  - set.med: 100

nodes:
  r1:
    routing.policy:
      p1:                   # Copy from global
