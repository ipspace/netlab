plugin: [ bgp.session ]
module: [ bgp ]

defaults.device: fortios

nodes:
  dut:
    bgp.as: 65000
    bgp.gr:
      state: enable
      restart_time: 0
  x:
    device: frr
    bgp.as: 65001

links:
- dut:
  x:
