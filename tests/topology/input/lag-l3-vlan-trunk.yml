#
# Basic L3 LAG with VLANs example - 3 member links
#

defaults:
 provider: clab
 device: frr

module: [ lag,vlan ]

vlans:
 v1:
 v2:

nodes: [ r1, r2 ]
links:
- group: lag_with_trunk
  type: lag
  vlan.trunk: [v1,v2]
  members: [ r1-r2, r1-r2, r1-r2 ]
