#
# BGP role (RFC 9234) plugin topology test
#
plugin: [ bgp.policy ]
module: [ bgp ]

defaults.device: frr
defaults.bgp.role.strict: true
provider: clab

nodes:
  isp:
    bgp.as: 65001
  customer:
    bgp.as: 65002
  peer:
    bgp.as: 65003

links:
- isp:
    bgp.role:
      name: provider
      strict: true
  customer:
    bgp.role: customer
- isp:
    bgp.role: peer
  peer:
    bgp.role: peer
