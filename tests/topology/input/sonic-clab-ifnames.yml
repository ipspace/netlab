#
# docker-sonic-vs maps its front-panel ports from the ethN netdevs present at boot
# (eth1 -> Ethernet0), so the containerlab endpoint name must be ethN.
#
provider: clab
nodes:
  s1:
    device: sonic
  s2:
    device: sonic
links: [ s1-s2 ]
