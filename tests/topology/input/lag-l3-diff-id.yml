#
# Basic L3 LAG example with different lag id on each node (instead of same at link level)
#

defaults:
 provider: clab
 device: sros

module: [ lag ]
nodes: [ r1, r2 ]
links:
- r1:
   lag.id: 1
  r2:
   lag.id: 2
- r1:
   lag.id: 1
  r2:
   lag.id: 2