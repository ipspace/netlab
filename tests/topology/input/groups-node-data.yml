defaults:
  device: cumulus
  
module: [ bgp ]

defaults.bgp.as: 65000

nodes: [ a,b,c,d,e,f ]

vlans:
  vlan1000:

groups:
  g1: [ a,b,c ]
  g2:
    members: [ d,e,f ]
    module: [ vlan ]
    node_data:
      bgp.as: 65001
      vlans:  # Pull in global vlans
       vlan1000:
