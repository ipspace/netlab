---
defaults.device: eos
defaults.tests.ignore: [ addressing, defaults, includes, unmanaged ]

nodes:
  r1:
  h1:
    device: linux
  external:
    unmanaged: true

links:
- r1:
  external:
  role: external
- r1:
  h1:
  type: lan
