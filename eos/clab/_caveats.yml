ospf:
  ospfv3:
    06-lb-prefix:
      caveat: EOS advertises IPv6 loopback prefixes with /64 prefix length (see RFC 5340, section 4.4.3.9)
  gateway:
    02-vrrp:
      caveat: Shared VRRP IP address stops working on an Arista EOS switch with no VRRP peer
