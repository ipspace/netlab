{#
  vJunos-switch (Junos) VXLAN configuration

  Platform-specific: 'protocols evpn encapsulation vxlan' required for validation.
#}
{% if vxlan.flooding|default('') == 'evpn' %}

{# need to define this to avoid validation errors #}
protocols evpn encapsulation vxlan;

{% endif %}
{# Common Junos VXLAN configuration (ECMP, switch-options, vlans, flooding) #}
{% include 'junos-common.j2' %}
