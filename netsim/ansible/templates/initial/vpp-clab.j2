#!/bin/sh
#
# Initial configuration for VPP containers (host-side netns execution).
#
set -x
set -e
{% include 'linux-clab.j2' +%}
#
{% if loopback.ipv6 is defined %}
set +e
ip addr add fe80::1/64 dev lo scope link
{% endif %}
exit 0
