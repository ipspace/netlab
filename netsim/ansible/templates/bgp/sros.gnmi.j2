# Generated by sros.gnmi.j2 template
updates:
- path: configure/router[router-name=Base]
  val:
   router-id: "{{ loopback['ipv4']|ipaddr('address') }}"
   autonomous-system: {{ bgp.as }}
   bgp:
    ebgp-default-reject-policy:
     import: False
     export: False
    multipath:
     ibgp: 64
     ebgp: 64
    min-route-advertisement: 1 # Be aggressive about sending updates
    connect-retry: 5           # Retry frequently, suitable in DC context
    client-reflect: {{ 'True' if bgp.rr|default('') else 'False' }}
    # family: cannot disable this
    # ipv4: False # Enabled by default, disable globally and set per group
    group:
{%  for c in ['ebgp','ibgp-ipv4']+(['ibgp-ipv6'] if 'ipv6' in loopback else []) %}
{%  set bgp_type=c[:4] %}
    - group-name: "{{c}}"
      admin-state: enable
      family:
       ipv4: {{ 'ipv4' in bgp and 'ipv4' in bgp.address_families[bgp_type] }}
       ipv6: {{ 'ipv6' in bgp and 'ipv6' in bgp.address_families[bgp_type] }}
      import:
       policy: ["accept_all"]
      export:
       policy: ["accept_all"]
{%    if bgp.community[ bgp_type ]|default([]) %}
      send-communities:
{%     if not ('standard' in bgp.community[ bgp_type ] or 'both' in bgp.community[ bgp_type ]) %}
       standard: False
{%     endif %}
{%     if not ('extended' in bgp.community[ bgp_type ] or 'both' in bgp.community[ bgp_type ]) %}
       extended: False
{%     endif %}
{%     if not ('large' in bgp.community[ bgp_type ]) %}
       large: False
{%     endif %}
{%    endif %}
{%    if c!='ebgp' %}
      local-address: "{{ loopback[ c[5:] ]|ipaddr('address') }}"
{%    if bgp.rr|default('')|bool %}
      cluster:
        cluster-id: "{{ loopback['ipv4']|ipaddr('address') }}"
{%    elif bgp.next_hop_self|default(false) %}
      next-hop-self: True
{%    endif %}
{%    endif %}

{%  endfor %}
    neighbor:
{%  for n in bgp.neighbors %}
{%  for af in ['ipv4','ipv6'] if n[af] is defined and bgp.peering[n.type][af] %}
    - ip-address: "{{ n[af] }}"
      description: "{{ n.name }}"
      peer-as: {{ n.as }}
      group: "{{ 'ebgp' if n.type=='ebgp' else (n.type+'-'+af) }}"
{%  endfor %}
{%  endfor %}

- path: configure/policy-options/policy-statement[name=accept_all]
  val:
   default-action:
    action-type: accept
