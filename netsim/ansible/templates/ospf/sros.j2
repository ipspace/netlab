{% set pid = ospf.process|default(0) %}
{% set area = ospf.area|default("0.0.0.0") %}

updates:
{# Tried using openconfig:
- path: openconfig-network-instance:network-instances/network-instance[name=Base]/protocols
  val:
   protocol:
   - identifier: "OSPF"
     name: "0"
     config:
      identifier: "OSPF"
      name: "0"
      enabled: True

However: MINOR: OPENCONFIG #238: configure openconfig network-instances network-instance "Base" protocols protocol OSPF name "0"
       - Configuration change failed validation - Invalid identifier. Supported values are ISIS BGP STATIC LOCAL_AGGREGATE IGMP
       ( on 21.10.R1 )
#}

- path: configure/router[router-name=Base]
  val:
   router-id: "{{ loopback['ipv4']|ipaddr('address') }}"
   ospf:
   - ospf-instance: {{ pid }}
     area:
     - area-id: "{{ area }}"
