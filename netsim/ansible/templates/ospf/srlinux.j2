{% set pid = ospf.process|default(0) %}
{% from "srlinux-module.j2" import ospf_config %}
updates:
{% if ospf.af.ipv4 is defined %}
{{ ospf_config( pid,'ipv4','default',ospf) }}
{% endif %}
{% if ospf.af.ipv6 is defined %}
{{ ospf_config( pid+1,'ipv6','default',ospf) }}
{% endif %}
