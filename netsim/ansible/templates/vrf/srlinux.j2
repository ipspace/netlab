{% from "templates/ospf/srlinux-module.j2" import ospf_config %}
updates:
{% for vname,vdata in vrfs.items() if 'ospf' in vdata %}
{{ ospf_config(0,'ipv4' if vdata.af.ipv4|default(0) else 'ipv6',vname,vdata.ospf)}}
{% endfor %}
