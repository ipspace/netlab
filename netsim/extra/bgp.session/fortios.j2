{# Empty on purpose: all bgp.session attributes are rendered in the bgp/fortios.j2
   module template (cf. openbsd.j2). The bgp.session plugin copies the attributes
   onto bgp.neighbors at transform time, so the module template -- which is always
   deployed, even when no per-neighbor session attribute schedules this snippet --
   sees the same data. #}
