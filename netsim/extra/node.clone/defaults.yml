# node.clone plugin attributes and defaults
#
---
clone:
  # Result should be a valid identifier, can use '-' in node names
  node_name_pattern: "{name[:13]}-{id:02d}"

#
#  As the plugin runs before attribute validation, this structure is never
#  checked. Left for documentation purposes
#
#  attributes:
#    node:
#      # Create N copies of this node anywhere it's used in groups or links
#      count: { type: int, min_value: 1, _required: True }
#      start: int          # Optional ID to start with, default 1
#      step: int           # Optional increment between clones, default 1
