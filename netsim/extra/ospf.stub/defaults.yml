---
devices:
  cumulus_nvue.features.ospf:
    stub: True
  dellos10.features.ospf:
    stub: True
  frr.features.ospf:
    stub: True
  none.features.ospf:
    stub: True

ospf:
  attributes:
    global:
      areas:
        type: dict
        _requires: [ ospf ]
        _keytype: int
        _subtype:
          kind: { 'type': str, 'valid_values': [ stub, nssa, normal ] }
          no_summary: bool                                                      # Only applied at ABR
          range: { 'type': list, _subtype: { type: ipv4, use: subnet_prefix } } # Only for 'normal' areas
    node:
      areas:
