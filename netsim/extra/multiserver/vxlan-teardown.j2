#!/bin/bash
# VXLAN tunnel teardown — generated by netlab multiserver plugin
# Run BEFORE: sudo clab destroy -t clab.yml
set -e

{% for t in tunnels %}
ip link del vxlan{{ t.vni }} 2>/dev/null && echo "  deleted vxlan{{ t.vni }}" || true
{% endfor %}

echo "VXLAN teardown complete."
