# repeater plugin attributes and defaults
#
---
attributes:
  node:
    repeat: int                     # Create N copies of this node anywhere it's used in groups or links

repeater:
  node_name_pattern: "{name}-{id}"