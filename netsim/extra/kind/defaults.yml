# Defined KinD daemons (cluster and node)
#
---
daemons:
  kind-node:
    parent: linux
    description: Node in a KinD cluster
    clab:
      kind: ext-container
      features:
        lag: false
        vlan: false
      roles: [ host ]
      node_config:
        initial: /etc/01-initial.sh:cp_sh
        routing: /etc/02-routing.sh:cp_sh
