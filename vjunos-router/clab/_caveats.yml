bgp:
  05-community:
    caveat: |
      Junos has no (easy) mechanism to control community propagation on BGP sessions

vlan:
  70-vlan-1-trunk:
    caveat: |
      netlab Junos router configuration template cannot configure tagged VLAN 1
      in a VLAN trunk
